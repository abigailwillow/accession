name: Unity - Build
on: 
  workflow_dispatch:
  push:
    tags:
    - '*'
jobs:
  build:
    name: Build project
    runs-on: ubuntu-latest
    environment: production
    steps:
      # Checkout
      - name: Checkout repository
        uses: actions/checkout@v2
        
      # Cache
      - name: Cache repository
        uses: actions/cache@v2
        with:
          path: Library
          key: Library-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-
      
      # Build
      - name: Build project
        uses: game-ci/unity-builder@v2
        id: build
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: StandaloneWindows64
          versioning: Semantic
          customParameters: -Development
          buildName: Accession
          
      # Upload
      - name: Upload build
        uses: actions/upload-artifact@v2
        with:
          name: Accession-${{ steps.build.outputs.buildVersion }}-win64
          path: build
          
      # Release
      - name: Create release
        uses: actions/create-release@v1
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.build.outputs.buildVersion }}
          release_name: ${{ steps.build.outputs.buildVersion }}
          prerelease: true
          
      # Assets
      - name: Upload release asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.release.outputs.upload_url }}
          asset_path: Accession-${{ steps.build.outputs.buildVersion }}-win64
          asset_name: Accession-${{ steps.build.outputs.buildVersion }}-win64
          asset_content_type: application/zip
